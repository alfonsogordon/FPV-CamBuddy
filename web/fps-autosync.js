(()=>{
'use strict';
const $=id=>document.getElementById(id);
const DEMO='freeclinkerDemoMode';
const BASE=new Set(['cameraType','cameraMatch','wakeGuard','stopOnDisarm','disarmDelay','debugBle','lowPower','wifiApEnabled','wifiApDelay']);
const AUX=new Set(['fpsAuxMaster','auxChannel','auxMode']);
const OSD_NATIVE=new Set(['pilotTpl','craftTpl','fpvFlash','fpvPreArm','fpvPreArmText','fpvCustomDurationSec','fpvLowPct','fpvRecLowMin']);
let needsRead=false,dirty=false;
function connected(){return !!$('connectBtn')?.disabled}
function demo(){return localStorage.getItem(DEMO)==='1'}
function managed(id){return BASE.has(id)||AUX.has(id)||id.startsWith('fps')||/^osd[1-4]$/.test(id)||OSD_NATIVE.has(id)}
function click(id){const b=$(id);if(b&&!b.disabled)b.click()}
function style(){const s=document.createElement('style');s.id='fpsExplicitSaveStyle';s.textContent='#fpsDeviceBar{display:flex;align-items:center;gap:10px;padding:9px 16px;background:#111827;color:#f9fafb;border-bottom:1px solid #374151;flex-shrink:0;box-shadow:0 2px 8px rgba(0,0,0,.18);z-index:20}#fpsDeviceBar .lbl{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:#9ca3af;font-weight:700}#fpsDeviceState{font-size:12px;margin-right:auto;color:#d1d5db;font-weight:700}#fpsDeviceBar button{min-width:150px;padding:7px 14px;font-weight:800;border-width:2px}#fpsTopRead{background:#374151;color:#fff;border-color:#6b7280}#fpsTopSave{background:#2563eb;color:#fff;border-color:#60a5fa}@keyframes readPulse{0%,100%{background:#7f1d1d;box-shadow:0 0 0 0 rgba(239,68,68,.2)}50%{background:#dc2626;box-shadow:0 0 0 8px rgba(239,68,68,.35)}}@keyframes savePulse{0%,100%{box-shadow:0 0 0 0 rgba(59,130,246,.2)}50%{box-shadow:0 0 0 8px rgba(59,130,246,.3)}}#fpsTopRead.need{animation:readPulse .8s ease-in-out infinite;border-color:#fecaca}#fpsTopSave.need{animation:savePulse .9s ease-in-out infinite;border-color:#bfdbfe}';document.head.appendChild(s)}
function refresh(){const r=$('fpsTopRead'),s=$('fpsTopSave'),t=$('fpsDeviceState');if(r){r.disabled=!connected();r.classList.toggle('need',connected()&&needsRead)}if(s){s.disabled=!connected()||needsRead;s.classList.toggle('need',connected()&&!needsRead&&dirty)}if(t){if(!connected())t.textContent='Connect a C3 to read settings';else if(needsRead)t.textContent='New device connected — READ SETTINGS before editing';else if(dirty)t.textContent='Unsaved changes — SAVE / APPLY before disconnecting';else if(!t.textContent.includes('✓')&&!t.textContent.includes('…'))t.textContent='Settings ready'}}
function mark(e){if(e&&managed(e.id||'')&&!needsRead){dirty=true;refresh()}}
function bar(){const h=document.querySelector('header');if(!h||$('fpsDeviceBar'))return;const b=document.createElement('div');b.id='fpsDeviceBar';b.innerHTML='<span class="lbl">Device settings</span><span id="fpsDeviceState">Connect a C3 to read settings</span><button id="fpsTopRead" type="button" disabled>READ SETTINGS</button><button id="fpsTopSave" type="button" disabled>SAVE / APPLY SETTINGS</button>';h.insertAdjacentElement('afterend',b);$('fpsTopRead').onclick=()=>{if(!connected()||demo())return;click('readBtn');needsRead=false;dirty=false;refresh();const t=$('fpsDeviceState');t.textContent='Reading settings from C3…';setTimeout(()=>{if(connected()&&!needsRead&&!dirty)t.textContent='Settings read from C3 ✓'},1050)};$('fpsTopSave').onclick=()=>{if(!connected()||demo())return;const t=$('fpsDeviceState');t.textContent='Saving settings to C3…';click('applyBtn');setTimeout(()=>click('auxApplyBtn'),120);setTimeout(()=>click('fpsApplyAll'),240);dirty=false;refresh();setTimeout(()=>{if(connected()&&!dirty)t.textContent='Settings applied to C3 ✓'},1100)}}
function init(){style();bar();const r=$('readBtn');if(r)r.textContent='Read Settings';const a=$('fpsApplyAll');if(a)a.textContent='Save / Apply OSD Settings';const c=$('connectBtn');if(c){new MutationObserver(()=>{if(connected()){needsRead=true;dirty=false}else{needsRead=false;dirty=false}refresh()}).observe(c,{attributes:true,attributeFilter:['disabled']});if(connected())needsRead=true}$('readBtn')?.addEventListener('click',()=>{if(connected()&&!demo()){needsRead=false;dirty=false;refresh()}});document.addEventListener('change',e=>{if(e.isTrusted)mark(e.target)});document.addEventListener('input',e=>{if(e.isTrusted)mark(e.target)});document.addEventListener('click',e=>{if(!e.isTrusted)return;const btn=e.target?.closest?.('.fps-menu button,.fps-chips button');if(!btn)return;const input=btn.closest('.fps-builder')?.querySelector?.('input.fps-hidden-template, input[id^="osd"], input#pilotTpl, input#craftTpl');if(input)setTimeout(()=>mark(input),0)});refresh()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,700));else setTimeout(init,700);
})();
